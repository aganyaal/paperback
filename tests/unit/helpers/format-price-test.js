import { formatPrice } from 'page-turner/helpers/format-price';
import { module, test } from 'qunit';

module('Unit | Helper | format price');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatPrice([42]);
  assert.ok(result);
});
